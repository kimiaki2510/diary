<h1><%= @record.id %> : <%= @record.title%>の詳細ページ</h1>
<p><%= @record.content%></p>

<%= link_to '一覧に戻る', root_url%>
<%= link_to '編集する', edit_record_path(@record), class: 'btn btn-light' %>
<%= link_to '削除する', @record, method: :delete, data: {confirm: 'ほんまに消すけどええの？'}, class: 'btn btn-danger' %>
